spring:
  datasource:
    url: jdbc:tc:postgresql:15:///opsdb_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
  data:
    redis:
      host: localhost
      port: 6379
